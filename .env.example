# Copy this file to .env and fill in the values for your local environment.
# Never commit the actual .env file.

# ── Infisical Secrets Management ───────────────────────────────────────────────
# Set these to enable dynamic secret loading from Infisical at startup.
# When CLIENT_ID or CLIENT_SECRET are empty, secret loading is skipped and
# the application uses the environment variables defined in this file instead
# (useful for local development without an Infisical instance).
INFISICAL_SITE_URL=
INFISICAL_PROJECT_ID=
INFISICAL_UNIVERSAL_AUTH_CLIENT_ID=
INFISICAL_UNIVERSAL_AUTH_CLIENT_SECRET=

# ── Application ────────────────────────────────────────────────────────────────
APP_PORT=3000
APP_ENV=development

# ── Primary Database (PostgreSQL) ──────────────────────────────────────────────
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=payment
DATABASE_USER=postgres
DATABASE_PASSWORD=changeme
DATABASE_SSLMODE=disable

# ── ARI Database (PostgreSQL) ──────────────────────────────────────────────────
# ARI DB is managed externally by centra-backend-api-nodejs.
# Set these vars to point at the shared/external ARI DB instance.
ARI_DB_HOST=localhost
ARI_DB_PORT=5432
ARI_DB_NAME=ari
ARI_DB_USER=postgres
ARI_DB_PASSWORD=changeme
ARI_DB_SSLMODE=disable

# ── Redis ──────────────────────────────────────────────────────────────────────
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ── Vaultera PCI Proxy ─────────────────────────────────────────────────────────
VAULTERA_API_KEY=your_vaultera_api_key_here
VAULTERA_BASE_URL=https://pci.vaultera.co/api/v1
